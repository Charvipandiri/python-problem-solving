Problem: Complete the function replace_square() that takes a list and an integer N.
         Replace every element whose position is a multiple of N with its square.


def replace_cube(arr, n):
    for i in range(len(arr)):
        if (i + 1) % n == 0:
            arr[i] = arr[i] ** 3
    return arr     
